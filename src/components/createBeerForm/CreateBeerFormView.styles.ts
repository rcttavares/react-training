import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  paper: {
    padding: 20
  },
  typography: {
    fontWeight: 600,
    textAlign: 'center'
  },
  container: {
    marginTop: 10
  },
  button: {
    marginTop: 20,
    textAlign: 'center'
  }
});
