import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    paper: {
        padding: 20
    },
    title: {
        textAlign: 'center'
    },
    container: {
        marginTop: 10
    },
    button: {
        marginTop: 20,
        textAlign: 'center'
    }
});
